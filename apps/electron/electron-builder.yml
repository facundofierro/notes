appId: com.agelum.notes
productName: Agelum
artifactName: "${productName}-${version}-${arch}.${ext}"
directories:
  output: dist
files:
  - "src/**/*"
  - "package.json"
extraResources:
  - from: "standalone-build"
    to: "web-standalone"
asar: true
npmRebuild: false
mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
  category: public.app-category.developer-tools
win:
  target:
    - target: nsis
    - target: zip
linux:
  executableName: agelum
  target:
    - target: AppImage
    - target: deb
  category: Development
